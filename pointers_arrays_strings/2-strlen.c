#include "main.h"

/**
 * _strlen -returns the length of the string
 * @s: inputs the string to count
 *
 * return: lenght of string as int
 *
 *@s:
 */

int _strlen(char *s)

{
	int i = 0;

	while (s[i] != '\0');



	i++;

	return (i);

}

